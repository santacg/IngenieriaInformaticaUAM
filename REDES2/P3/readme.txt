Para poder ejecutar se necesitan instalar las librerias presentes en requirements.txt.

El simulador genera un ejemplo de prueba de forma autómatica limpiando la base de datos
y añadiendo modelos.

En el topic no se ha puesto la pareja y el grupo pero se puede cambiar con facilidad
editando la variable "topic" que es una variable global en cada dummy y en el controlador.